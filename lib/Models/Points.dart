

class PointsModel{
  int id;
  int total_points;
  int points_to_cash;
  int points;
  int converted;

  PointsModel(
      this.id, this.total_points, this.points_to_cash, this.points, this.converted);
}